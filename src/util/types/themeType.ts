export type ThemeType = {
  primaryFontFamily: string;
  secondaryFontFamily: string;

  primary: string;
  primaryText: string;
  secondary: string;
  secondaryText: string;

  dividerColor: string;

  breakpoints: {
    mobile: string;
  }
}